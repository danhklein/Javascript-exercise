//Write a function that returns ten lines.
// On the first line there is one '#' 
//character. On the second there are two. 
//And so on. This will create a nice triangle

//Refactor the above function (on a new branch) 
//so that it creates a triangle where the point
// is in the middle

function pyramid(tut) {
  var tut ="";
  var toot = [];
  for (var i = 1; i <=10; i++) {
     toot.push("#");
    for (var x = 0; x<=10; x++) {
    
  }
  return toot;
}
}